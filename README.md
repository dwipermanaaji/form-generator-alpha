## Form Generator Alpha Version
```bash
$ git clone https://github.com/dwipermanaaji/form-generator-alpha.git
$ cd form-generator-alpha
$ composer install
$ cp .env.example .env
$ php artisan key:generate
$ php aritsan migrate --seed
$ php artisan serve
```
